Typically you do the following where user name is usually ubuntu or ec2-user:

ssh -i MinaKey.pem  ubuntu@ec2-1-2-3.compute-1.amazonaws.com

instead you can put the information in the config file and specify the host and only do ssh host-naem
